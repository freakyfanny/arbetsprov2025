generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Application {
  id         String   @id @default(cuid())
  name       String
  email      String
  activities String 
  createdAt  DateTime @default(now())
}
